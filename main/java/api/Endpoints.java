package api;

import utils.ConfigReader;

public class Endpoints {
    public static String collections = "/collections";
    public static String createNewCollectionName = "/collections/{collectionName}/objects";
}
